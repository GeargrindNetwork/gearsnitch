name: GearSnitch
settings:
  base:
    DEVELOPMENT_TEAM: TUZYDM227C
targets:
  GearSnitch:
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.gearsnitch.app
    # Wrong merchant id (typo / pre-PR-#25 placeholder). Should fail item #13
    # check because the canonical id `merchant.gearsnitch.app` is missing.
    entitlements:
      path: GearSnitch/GearSnitch.entitlements
      properties:
        com.apple.developer.in-app-payments:
          - merchant.com.gearsnitch.app
